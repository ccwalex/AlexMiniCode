# python_checker (vendored)

Vendored copy of [ccwalex/agent_py_checker](https://github.com/ccwalex/agent_py_checker) for deterministic Python write/edit verification.

Loaded from `modules/python_checker/` via the agent's `modules/` `sys.path` entry. Rule packs live under `rules/`.
